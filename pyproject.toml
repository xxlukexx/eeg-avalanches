[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "eeg-avalanches"
version = "0.3.0"
description = "Sensor-space neuronal avalanche metrics for cleaned EEG arrays"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = ["numpy>=1.24"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Topic :: Scientific/Engineering :: Medical Science Apps.",
]

[project.optional-dependencies]
eeglab = ["mne>=1.6", "scipy>=1.10"]
test = ["pytest>=8"]

[project.scripts]
eeg-avalanches = "eeg_avalanches.cli:main"
eeg-avalanches-methods = "eeg_avalanches.methods:main"
leap-eeglab-to-npy = "eeg_avalanches.leap_eeglab:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.setuptools.packages.find]
where = ["src"]
